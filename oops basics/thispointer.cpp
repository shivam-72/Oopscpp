//with the help of this operator we can give the same name to
class rectangle
{ //the parameter which is the name of data in class
private:
    int length;
    int breadth;

public:
    rectangle(int length, int breadth)
    {
        this->length = length;
        this->breadth = breadth;
    }
};